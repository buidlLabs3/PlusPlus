[package]
name = "swap-executor"
version = "0.1.0"
edition = "2021"

[dependencies]
ckb-types = "0.119"
offer-protocol = { path = "../offer-protocol" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
blake2b-rs = "0.2"

[dev-dependencies]
tempfile = "3"
